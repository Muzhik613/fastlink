# grok-4.3 feedback build — 2026-09-15 (handoff)

Goal: 59/59 on the six hvm cells (multipage, staticform, overlay, extract, flightsearch, mapsdir) with grok-4.3 + phase2 by giving the tools richer feedback, not by changing the model. Owner: "4.3 can do it, it needs more feedback, we have to fix these tools."

Start point: hvm pass 4 (2026-09-15 06:30Z, grok-4.3 / phase2, old tools) = **52/59**: staticform 11/12 (!), extract 19/22 (!), flightsearch 8/10 (!), mapsdir 5/6 (!), multipage 6/6, overlay 3/3. Local relay bench: 56/70.

## What was built (WSL `main`, all pushed to hvm as `feedback-build` and fast-forwarded there)

| commit | change | what the tool now returns |
|---|---|---|
| `653f33e` | page.js: verified state, auto-wait, truncation-first, settled snapshots, `fast_key_press` in page.js (`key.js` deleted); tools.js + relay mirror | `fast_fill` → `{verified, value, filled…}` (live value after settle; `reason` if it did not hold). `fast_fill_form` verifies always: per-field `value`+`verified`, top-level `verified`+`reverted`. `fast_select_option` → `{verified, picked, value, field:{label,section,id…}, kind}` — names the dropdown it acted on. `fast_click` → `{clicked, url, urlChanged, dialogOpened/Closed, focused, …}`. Misses on fill/select/click retry the lookup for 1.5s, then return `waitedMs`, `settling` (page still mutating), `candidates`/`hiddenMatches`/`hint`. Every capped read STARTS with `truncated:true, dropped:{items,content,offscreen,textTrimmed}, hint` (explicit snapshot, auto-snapshot, viewport-only, `fast_text maxLen`). Auto-snapshots wait for DOM quiet (150ms, ≤1s) and flag `settling:true` otherwise. `fast_key_press` returns `target`, `url/urlChanged` + settled snapshot. |
| `dffa7c7` | runner: evidence gate, dated prompt, loud 80k cap | `report_done` refused (≤3×, then accepted + `gateOverridden`) unless a read tool ran after the last state-changing call AND `evidence` quotes a tool result verbatim; refusal text names what is missing. `gateRefusals[]` per run in `runs.jsonl`. System prompt: "Today is <weekday> <date> (America/Chicago)" + "never report from truncated:true". 80k cap prefixes `[truncated:true — …]`. |
| `999d58e` | phase2 toolset: −`fast_evaluate` (14 tools), descriptions with miss/truncation/wait/index guidance, `report_done` demands exact quotes | test asserts 14 tools + gate unit test |
| `f37fcf8` | index.js/text.js: page results cross `executeScript` as a JSON string | Chrome sorts returned object keys (base::Value dict) — `verified`/`truncated` landed after `snapshot`. String keeps order. `labelFor` strips a select's option text from a wrapping label. |
| `c12716f` | hvm rig: clear SW `ScriptCache` on Chrome launch; snapshot `hint` fix | Chrome served the OLD background worker across two restarts (content scripts were fresh). |
| `13bf127` | `toolset.phase2-eval.json` (phase2 + read-only evaluate) for the owner A/B; README | 15 tools; test asserts it differs from phase2 only by evaluate |

Verified on the hvm rig via the broker (selenium web-form): fill → `{"verified":true,"value":"hello",…}`, key_press → `{keyDispatched, target:{…}, url, urlChanged, snapshot}`, fast_text maxLen → `{"truncated":true,"dropped":{"chars":237},"hint":…}`, snapshot limit:3 → `{"truncated":true,"dropped":{"items":14},…}`, fill miss → 1534ms with candidates.

## Bench iterations (hvm, local transport, six cells; doc regenerated by the rig into `docs/GROK_RUNNER_BENCH_hvm_feedback_2026-09-15.md`, log `bench/hvm-feedback.log`, rows since 2026-09-15T07:06:06Z)

| iteration | build | model / toolset | result |
|---|---|---|---|
| 0 (before) | ba610e0 tools | 4.3 / phase2 | 52/59 — staticform 11/12 (textarea value not quoted), extract 19/22 ("US", World row → Mexico dropped), flightsearch 8/10 (Class + Airline never set; viewport-only snapshot hid them), mapsdir 5/6 (Enter returned nothing → reported "4 routes" never seen) |
| 1 | c12716f | 4.3 / phase2 | RUNNING when the PC hibernated (launched 07:06Z, pid 3036488). Read `docs/GROK_RUNNER_BENCH_hvm_feedback_2026-09-15.md` on hvm. |
| 2 (queued) | c12716f+13bf127 | 4.6 / phase2 | control for regression — queued by `bench/hvm-queue-feedback.sh` |
| 3 (queued) | same | 4.3 / phase2-eval | owner A/B: does evaluate help? count evaluate calls per cell in `runs.jsonl` (`histogram.fast_evaluate`), check whether each replaced a fast_text/snapshot read and whether the cell overclaimed |

Gate refusals: read `gateRefusals` per row in `/home/dev/.local/state/fastrun/runs.jsonl` (rows since 07:06Z) — not yet tallied.

## Known remaining risks / next steps
- **extract "World" row**: prompt says "top 10 entries", scorer skips the World aggregate; 4.6 also included World in 2 of 3 hvm runs. Not a tool defect — decide prompt wording ("countries") vs scorer; left untouched. The abbreviation half ("US", "1.429B") is addressed by the exact-quote `report_done` description.
- **mapsdir**: route render needs a read after Enter; `fast_key_press` now returns a settled snapshot and the gate forces a read-back. If distances still miss, add a `fast_wait "min"`-style hint to the key_press result when `settling:true`.
- **flightsearch**: below-the-fold selects now show up as `dropped.offscreen` on every viewport snapshot; if 4.3 still skips Class/Airline, strengthen the auto-snapshot hint to list the off-screen field labels.
- gcpform / cfworkers cannot run on hvm (no login); their fixes (fill on not-yet-rendered field, cfc-select) are covered by auto-wait + candidates but unverified.
- Windows extension copy NOT synced by this work (other agent); relay NOT deployed (`fastlink-relay/tools.js` mirrored only).
- Rig gotcha: a Chrome restart does not reload the extension's service worker unless `Default/Service Worker/ScriptCache` is cleared (now done by `hvm-rig.sh`).
