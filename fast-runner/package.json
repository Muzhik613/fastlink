{
  "name": "fast-runner",
  "private": true,
  "type": "module",
  "description": "Grok (grok-4.6) drives the browser end to end through FastLink's MCP tools; a caller (Claude) dispatches tasks and answers questions.",
  "bin": { "fastrun": "./cli.mjs" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
